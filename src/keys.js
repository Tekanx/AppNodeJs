const { userInfo } = require("os")
const passport = require("passport")

module.exports = {
    database:{
        host: 'localhost',
        user: 'root',
        password: '',
        database: 'db_links'
    }
}